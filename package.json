{
  "name": "boardgame-test",
  "version": "0.0.1",
  "description": "a prototype of a board game server",
  "main": "main.ts",
  "scripts": {
    "build": "tsc --outDir dist -p ./tsconfig.json",
    "start": "node dist/main.js",
    "watch": "tsc --outDir dist -p ./tsconfig.json --watch",
    "clean": "rm -rf dist",
    "eslint": "eslint src/*.ts",
    "esfix": "eslint --fix src/*.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.7",
    "eslint": "^8.46.0",
    "eslint-config-google": "^0.14.0",
    "typescript": "^5.1.6"
  }
}
