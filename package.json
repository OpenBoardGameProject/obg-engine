{
  "name": "obg-engine",
  "version": "0.0.1",
  "description": "the core engine of obg project",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "install": "npm run build",
    "build": "tsc --outDir dist",
    "start": "node dist/main.js",
    "watch": "tsc --outDir dist -p ./tsconfig.json --watch",
    "clean": "rm -rf dist",
    "eslint": "eslint src/*.ts",
    "esfix": "eslint --fix src/*.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.7",
    "eslint": "^8.46.0",
    "eslint-config-google": "^0.14.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
