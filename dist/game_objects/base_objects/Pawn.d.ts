import { DataObject } from "../interfaces";
import { Item } from "./Item";
import { PawnConfig } from "../../config/config_types";
import { Color } from "../../engine/environments";
import { Vector2D } from "../../engine/math";
import { Tile } from "../../engine/tile";
import { GameObject } from "../GameObject";
declare class Pawn extends GameObject implements DataObject {
    color: Color;
    log_tag?: string;
    hasBeenPlayed: boolean;
    config: PawnConfig;
    item?: Item;
    _health: number;
    get health(): number;
    constructor(config: PawnConfig, color: Color);
    toString(): string;
    toRepr(): string;
    get has_item(): boolean;
    is_attackable(incoming: Pawn): boolean;
    canMove(src: Vector2D, dst: Vector2D): boolean;
    canAttack(src: Vector2D, dst: Vector2D): boolean;
    is_dead(): boolean;
    equip(item: Item): void;
    get attack(): number;
    get defense(): number;
    processIncomingAttack(incoming: Pawn): void;
    processIncomingDefense(object: Pawn): void;
    processDeath(current?: Tile): void;
    processNewTurn(): void;
    processMove(): void;
}
export { Pawn };
